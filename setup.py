from setuptools import setup

setup(
    name="man-io",
    version="0.1.0",
    packages=["man"],  # Explicitly include only the 'man' package
    install_requires=[
        # Add your Python dependencies here
    ],
)
